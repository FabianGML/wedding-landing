---
interface Props {
    title: string;
    background?: string;
}

const { title } = Astro.props;
---

<section class="w-full max-w-4xl mx-auto py-24">
    <h3 class="text-8xl text-center md:text-start">{title}</h3>
    <slot />
</section>
